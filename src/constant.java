public class constant {

    public static void main(String[] args){

       final float PI = 3.14F; // by convention we use all capital letters to name contants so PI
      //pi =1;
        System.out.println(PI);// if we assign pi =1 all our calculations will messed up so , we use final to treat pi value as a constant that we cannot change it
    }
}
